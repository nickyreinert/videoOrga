# Core dependencies
opencv-python>=4.8.0
pillow>=10.0.0



# Transformers and AI models
transformers==4.57.1  # Upgraded for LLaVA compatibility
tokenizers==0.19.1    # Corresponding tokenizer version
accelerate==1.11.0
bitsandbytes==0.48.2  # For 4-bit model quantization (e.g., Mistral)

# Audio transcription
openai-whisper  # Install separately if you want audio transcription
# NOTE: Whisper requires FFmpeg to be installed on your system

# Optional: For BLIP-2 support
# salesforce-lavis

# Utilities
tqdm>=4.65.0

# Web Interface
flask>=2.3.0
flask-cors>=4.0.0
humanize>=4.8.0

# Translation support for tags 
sentencepiece>=0.2.0

sacremoses

# Note: FFmpeg must be installed on your system for audio extraction
# Linux: sudo apt install ffmpeg
# Mac: brew install ffmpeg
# Windows: Download from https://ffmpeg.org/