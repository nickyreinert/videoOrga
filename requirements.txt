# Core dependencies
opencv-python>=4.8.0
pillow>=10.0.0

# PyTorch (install separately with CUDA support)
# See installation instructions in README.md
# torch>=2.0.0
# torchvision>=0.15.0

# Transformers and AI models
transformers==4.36.2  # Pinned to a version compatible with torch 2.1.0
accelerate>=0.20.0

# Audio transcription
openai-whisper  # Install separately if you want audio transcription
# NOTE: Whisper requires FFmpeg to be installed on your system

# Optional: For BLIP-2 support
# salesforce-lavis

# Utilities
tqdm>=4.65.0

# Web Interface
flask>=2.3.0
flask-cors>=4.0.0
humanize>=4.8.0

# Translation support for tags 
sentencepiece=>=0.1.99

# Note: FFmpeg must be installed on your system for audio extraction
# Linux: sudo apt install ffmpeg
# Mac: brew install ffmpeg
# Windows: Download from https://ffmpeg.org/